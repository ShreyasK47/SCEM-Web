export default function Promotion({imgUrl}) {
    return (
      <section id="Promotion">
        <div className="h-[300px] w-full">
          <img src={imgUrl} />
        </div>
      </section>
    );
  }